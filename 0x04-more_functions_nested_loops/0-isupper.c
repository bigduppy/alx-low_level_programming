#include "main.h"

/**
 * _isupper - Check for uppercase
 * @c: The letter to be checked
 * Return: 1 for uppercase, 0 for anything else
 */

int _isupper(int c)

{
	if (c >= 65 && c <= 90)
	{
	return (0);
	}
	return (1);
}
