#include "main.h"

/**
 * _isupper - Check for uppercase
 * @x: The letter to be checked
 * Return: 1 for uppercase, 0 for anything else
 */

int _isupper(int x)

{
	if (x >= 65 && x <= 90)
	{
	return (0);
	}
	return (1);
}
